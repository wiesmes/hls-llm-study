# 2026-09-15

## Goal
Get vecadd running on the Arty Z7-20 over micro-USB (bare-metal, no
Ethernet or SD card).

## Done
- Prof. Naher confirmed an Ethernet cable is not needed for the demo,
  so switched from the PYNQ plan to bare-metal over JTAG/UART.
- Vivado reported synthesis/implementation out of date (design_1.bd
  had been touched since the Sep 8 build). Regenerated the bitstream.
  Timing met: WNS 1.314 ns.
- Exported hardware with bitstream:
  `C:/Users/wwa228/vivado_ws/vecadd_arty/design_1_wrapper.xsa`
- Opened Hardware Manager, auto-connected over JTAG
  (Digilent/003017BFAECCA), programmed `design_1_wrapper.bit`.
  DONE LED lit. Log:

```
program_hw_devices [get_hw_devices xc7z020_1]
INFO: [Labtools 27-3164] End of startup status: HIGH
INFO: [Labtools 27-1434] Device xc7z020 (JTAG device index = 1) is
programmed with a design that has no supported soft debug core(s) in it.
```

"No debug cores" is expected; there is no ILA in the design.

## Blocked
- Vitis Unified IDE 2025.2 on this machine has only the HLS component.
  File > New Component shows HLS only, no Platform or Application.
  Vitis Embedded Development (ARM cross-compiler, BSP generator, xsct
  loader) must be added via `C:\AMDDesignTools\2025.2\xsetup.exe`
  > Add Design Tools or Devices > Vitis > Vitis Embedded Development.
  Needs admin approval.

## Also today
- Set JP4 to JTAG for programming over USB. Blank 32 GB microSD is in
  the slot but unused.
- Prof. Naher is sourcing an Ethernet cable, which reopens PYNQ as an
  option later (still need a card reader).

## Next session
1. Install Vitis Embedded Development (30 to 60 min).
2. Platform component from `design_1_wrapper.xsa`, standalone,
   ps7_cortexa9_0.
3. Hello World application, run, confirm UART output at 115200.
4. Add `xvecadd` driver files from
   `vitis_ws\vecadd\vecadd\hls\impl\ip\drivers\vecadd_v1_0\src`,
   write the driver program, run, verify 256 results.
5. Then start the research proper: naive GEMM 32x32 int16 + testbench
   through `make bench`.